{
  "type": "postgres",
  "port": 5432,
  "username": "docker",
  "password": "ignite",

  "name": "default",
  "host": "localhost",
  "database": "fin_api",
  "entities": [
    "./src/modules/**/entities/*.ts"
  ],
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
